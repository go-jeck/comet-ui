<h1> Dashboard </h1>

<ul>
  <% @applications.each do |app| %>
    <li><%= button_to("#{app.name} + #{app.namespace}", dashboard_index_path, method: :get) %></li>
  <% end %>
</ul>

<hr>
<br>
<br>
<%= button_to("Log Out", "/logout", method: :get) %>
